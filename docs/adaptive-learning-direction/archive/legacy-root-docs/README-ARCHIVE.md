# Legacy root documents

Các file trong thư mục này trước đây nằm trực tiếp trong `docs/`. Chúng đã được
lưu trữ vì không còn được dùng cho hướng project hiện hành.

File `README.md` trong thư mục này là README cũ của `docs/`, không phải hướng dẫn
hiện hành.
